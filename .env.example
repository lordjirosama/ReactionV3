# ═══════════════════════════════════════════════════
# Alisa Reaction Bot — Environment Configuration
# ═══════════════════════════════════════════════════

# ─── Required ──────────────────────────────────────

# Telegram Bot API token (get from @BotFather)
BOT_TOKEN=8658849738:AAFzvmhsJJLyEx82GtXNOwr06MqlKEbE08c

# Your bot's username (without @)
BOT_USERNAME=ProAutoReaction_bot

# ─── Multi-Bot Mode (optional — overrides BOT_TOKEN + BOT_USERNAME) ──

# Comma-separated bot tokens with usernames.
# Format: token1:username1,token2:username2
# Each bot gets its own webhook path: /bot/<username>
# Example:
# BOT_TOKENS=123:BotAlpha,456:BotBeta
# BOT_TOKENS=

# ─── Reactions ─────────────────────────────────────

# List of emojis the bot will use for reactions
# Example: 👍❤🔥🥰👏😁🎉🤩🙏👌🕊😍🐳❤‍🔥💯⚡🏆
EMOJI_LIST=👍❤🔥🥰👏😁🎉🤩🙏

# Force Subscribe (optional) — users must join these channels to use the bot
# Comma-separated channel usernames (public) or IDs (private)
# Public: @YourChannel | Private: -1001234567890
# Only applies in private chats, owner is exempt
# Bot must be admin in each channel
# FORCE_SUBSCRIBE_CHANNELS=@YourChannel,-1001234567890

# ─── Behavior ──────────────────────────────────────

# Randomness level for group chat reactions (0-10)
# 0 = react to every message (default)
# 10 = very random, rarely react
RANDOM_LEVEL=0

# Comma-separated list of chat IDs where bot should NOT react
# Example: -1001234567890,-1009876543210
RESTRICTED_CHATS=

# ─── Security ──────────────────────────────────────

# Bot owner's Telegram user ID (for /broadcast, /log commands)
# Get your ID: send /start to @userinfobot
OWNER_ID=7754709357

# Webhook secret token for validating incoming requests
# Generate: openssl rand -hex 32
# Set via BotFather: /setwebhook → secret_token parameter
WEBHOOK_SECRET=

# ─── Server ────────────────────────────────────────

# Bot photo URL (sends with /start, /help, /about, /stats, /donate, /reactions)
# Must be a valid URL or Telegram file_id
BOT_PHOTO=https://graph.org/file/fdc4357abfaba23255e98-24d1bbfa3888cdfcfe.jpg

# Server port (for Docker/VPS deployments)
PORT=3000

# Node environment
NODE_ENV=production

# ─── Persistent Storage (Redis) ───────────────────

# Upstash Redis (FREE — 10,000 req/day, 256MB)
# Sign up at https://console.upstash.com, create a Redis database,
# then copy the REST URL and TOKEN below.
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
